import { KeystoneTodoListExampleRoot } from './todoListExample/views';
import * as React from 'react';

export default {
  title: 'keystone',
};

export const todoList = () => {
  return <KeystoneTodoListExampleRoot />;
};
